var mapnik = require('./lib/mapnik');
var fs = require('fs');
var vt = require('./vtile').Tile;

var vtile = new mapnik.VectorTile(6, 32, 21);
//var incoming = fs.readFileSync('./6-32-21.mvt');
var incoming = fs.readFileSync('./test/data/vector_tile/10.196.370.vector.pbf');
var Pbf = require('pbf');
var obj = vt.read(new Pbf(incoming));
console.log(JSON.stringify(obj, null, ' '));
var pbf = new Pbf(incoming);
console.log('pbf fields:');
pbf.readFields(function(tag) {
    console.log(tag);
});
